# Bout Labs Website — Glossary

Project vocabulary. Each term gets one line.

(Empty by design — fill in as the project develops its own language. Agents
will reference this when interpreting issue bodies or PR descriptions.)

## Domain terms

## Technical terms

- **Capsule** — Capsule CRM; receives contact-form submissions via webform key
- **html-proofer** — link/asset checker run by `bundle exec rake test`
- **minima** — the Jekyll theme this site is built on (`~> 2.0`)

## Names with history

(Things that used to mean something different. Renames, deprecated terms,
short-lived experiments — note them here so future readers know what an old
issue is referring to.)

- **Contact page** — removed in commit `4653d48` ("Remove contact page");
  contact references stripped in `23754e0`. Don't try to "restore" the
  contact page based on stale references in old branches.
